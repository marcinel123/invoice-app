import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1f213a",
      light: "#fefefe",
      dark: "#141625",
    },
    secondary: {
      main: "#9277FF",
      light: "#7C5DFA",
    },
  },
});
